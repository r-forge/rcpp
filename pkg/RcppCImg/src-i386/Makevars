## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()" ) -lm -lpthread -L/usr/X11R6/lib -lX11
PKG_CPPFLAGS += -I../inst/include/
